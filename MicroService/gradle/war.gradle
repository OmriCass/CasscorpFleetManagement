apply plugin: "war"

bootWar {
    mainClass = "com.casscorp.fleetmanagement.MicroServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
